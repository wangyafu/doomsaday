"""
提示词模板和上下文组织模块

设计原则：
1. 每个AI角色有独立的系统提示词，定义其人格和输出格式
2. 上下文组织：截取最近3-5天历史，避免token浪费
3. 结构化输出：要求AI返回JSON格式，便于解析
"""
from app.models import Stats, InventoryItem, HistoryEntry, Shelter


# ==================== 上下文组织工具函数 ====================

def format_stats(stats: Stats) -> str:
    """格式化玩家状态"""
    return f"❤️生命: {stats.hp}/100 | 🍔饱腹: {stats.hunger}/100 | 🧠理智: {stats.san}/100"


def format_inventory(inventory: list[InventoryItem]) -> str:
    """格式化背包物品"""
    if not inventory:
        return "背包空空如也"
    items = [f"- {item.name} x{item.count}" for item in inventory]
    return "\n".join(items)


def format_history(history: list[HistoryEntry], max_days: int = 5) -> str:
    """
    格式化历史记录，只取最近N天
    这是关键的上下文压缩策略，避免token爆炸
    """
    if not history:
        return "这是末世的第一天..."
    
    # 只取最近的max_days天
    recent = history[-max_days:] if len(history) > max_days else history
    
    lines = []
    for entry in recent:
        result_tag = f" [{entry.event_result}]" if entry.event_result != "none" else ""
        lines.append(f"【第{entry.day}天】{entry.log}{result_tag}")
    
    return "\n".join(lines)


def format_hidden_tags(tags: list[str]) -> str:
    """格式化隐藏标签（仅供AI参考，不展示给玩家）"""
    if not tags:
        return "无"
    return ", ".join(tags)


# ==================== Narrator 提示词 (小说家/旁白) ====================
# 拆分为两步：
# 1. NARRATOR_NARRATIVE_SYSTEM_PROMPT - 叙事输出（流式，自由文本）
# 2. NARRATOR_STATE_SYSTEM_PROMPT - 状态更新（JSON模式）

NARRATOR_NARRATIVE_SYSTEM_PROMPT = """你是一位末世生存游戏的旁白，扮演"小说家"角色。

## 你的人格特点
- 擅长环境描写和氛围营造
- 善于制造悬疑感和紧张感
- 注重前后剧情的连贯性
- 文字简洁有力，像日记体

## 你的任务
根据玩家当前状态和历史，生成今天的生存日志。

## 输出格式
直接输出日志内容，不要使用JSON格式，不要有任何前缀或标记。

如果今天有危机事件需要玩家决策，在日志末尾用以下格式列出选项：
---
A. 选项1描述
B. 选项2描述
C. 选项3描述
D. 选项4描述

如果今天没有危机事件，就不要输出选项部分。

## 重要规则
- 日志内容100-200字，生动描写今天发生的事
- 选项要基于玩家当前拥有的物品，合理且有创意
- 注意玩家的隐藏标签，它们会影响剧情走向
- 物资充足时可以描写安心感，物资匮乏时要制造焦虑
- 理智(SAN)低时，描述要更加阴郁、出现幻觉暗示
- 发挥你的创造力，让叙事引人入胜！"""


NARRATOR_STATE_SYSTEM_PROMPT = """你是一个游戏状态计算器。

## 你的任务
根据今天的叙事内容，计算玩家状态和物品的变化。

## 输出格式要求
必须返回以下JSON格式（不要有其他内容）：
```json
{
  "stat_changes": {
    "hp": 0,
    "san": 0,
    "hunger": -10
  },
  "item_changes": {
    "remove": [{"name": "物品名", "count": 1}],
    "add": [{"name": "新物品", "count": 1}]
  },
  "new_hidden_tags": ["tag1"]
}
```

## 计算规则
- 每天基础消耗：hunger -10（饥饿度下降）
- 物品消耗要合理（如吃了食物才能恢复饱腹度）
- hidden_tags用于记录影响后续剧情的状态（如"受伤"、"生病"等）
- 如果叙事中没有提到物品使用或获得，item_changes的remove和add应为空数组"""


def build_narrator_prompt(
    day: int,
    stats: Stats,
    inventory: list[InventoryItem],
    hidden_tags: list[str],
    history: list[HistoryEntry],
    shelter: Shelter | None = None
) -> str:
    """
    构建Narrator的用户提示词
    核心：组织上下文，让AI理解当前游戏状态
    """
    shelter_info = "无避难所（露宿街头）"
    shelter_hidden_info = "无"
    if shelter is not None:
        defense_stars = "★" * max(1, int(shelter.defense or 1))
        shelter_info = (
            f"{shelter.name} (ID: {shelter.id})\n"
            f"- 防御: {defense_stars}\n"
            f"- 空间: {shelter.space}\n"
            f"- 说明: {shelter.description}"
        )
        shelter_hidden_info = shelter.hidden_discription or "无"
    
    return f"""## 当前游戏状态

**第 {day} 天**

### 避难所
{shelter_info}

### 避难所隐藏信息（仅供AI参考，玩家不可见）
{shelter_hidden_info}

### 玩家状态
{format_stats(stats)}

### 背包物品
{format_inventory(inventory)}

### 隐藏标签（影响剧情，玩家不可见）
{format_hidden_tags(hidden_tags)}

### 最近几天的经历
{format_history(history)}

---
请基于以上信息，生成第{day}天的生存日志。"""


def build_narrator_state_prompt(
    day: int,
    stats: Stats,
    inventory: list[InventoryItem],
    narrative_context: str
) -> str:
    """
    构建Narrator状态更新的用户提示词
    注意：此函数仅在无危机事件时调用，有危机时状态更新在Judge之后
    """
    return f"""## 状态更新计算

**第 {day} 天**

### 今日叙事内容
{narrative_context}

### 玩家当前状态
{format_stats(stats)}

### 玩家背包
{format_inventory(inventory)}

---
请根据今日叙事内容，计算状态和物品变化。"""


# ==================== Judge 提示词 (冷酷裁判/DM) ====================
# 拆分为两步：
# 1. JUDGE_NARRATIVE_SYSTEM_PROMPT - 叙事输出（流式，自由文本）
# 2. JUDGE_STATE_SYSTEM_PROMPT - 状态更新（JSON模式）

JUDGE_NARRATIVE_SYSTEM_PROMPT = """你是一位末世生存游戏的裁判，扮演"冷酷DM"角色。

## 你的人格特点
- 公正但严苛，不会轻易让玩家得逞
- 逻辑严密，判定基于物品和数值
- 喜欢给出意想不到但合理的结果
- 对创意行动会给予奖励

## 你的任务
玩家做出了行动选择，你需要判定结果并生成叙事描述。

## 输出格式
直接输出判定结果的叙事描述（50-150字），不要使用JSON格式。
描述要生动，让玩家感受到行动的后果。

## 判定原则
- 没有对应物品却想使用 → 失败，可能受伤
- 创意行动且有物品支持 → 成功，可能有额外奖励
- 鲁莽行动 → 失败或部分成功，大概率受伤
- 发挥你的创造力，让判定结果引人入胜！"""


JUDGE_STATE_SYSTEM_PROMPT = """你是一个游戏状态计算器。

## 你的任务
根据刚才的判定叙事，计算玩家状态和物品的变化，并给出评分。

## 输出格式要求
必须返回以下JSON格式（不要有其他内容）：
```json
{
  "score": 85,
  "stat_changes": {
    "hp": -10,
    "san": 5,
    "hunger": 0
  },
  "item_changes": {
    "remove": [{"name": "物品名", "count": 1}],
    "add": [{"name": "新物品", "count": 1}]
  },
  "new_hidden_tags": ["tag1"]
}
```

## 计算规则
- score: 行动评分0-100，创意行动高分，鲁莽行动低分
- stat_changes: 根据判定结果计算，受伤扣hp，恐惧扣san等
- item_changes: 消耗的物品放remove，获得的物品放add
- new_hidden_tags: 新增的状态标签，如"injured"、"infected"等"""


def build_judge_narrative_prompt(
    event_context: str,
    action_content: str,
    stats: Stats,
    inventory: list[InventoryItem],
    history: list[HistoryEntry]
) -> str:
    """构建Judge叙事阶段的用户提示词"""
    return f"""## 当前情境

### 事件描述
{event_context}

### 玩家选择的行动
{action_content}

### 玩家当前状态
{format_stats(stats)}

### 玩家背包（判定时参考）
{format_inventory(inventory)}

### 近期经历（供参考）
{format_history(history, max_days=3)}

---
请判定玩家行动的结果，生成叙事描述。
注意：如果玩家想用某物品但背包里没有，判定失败。"""


def build_judge_state_prompt(
    event_context: str,
    action_content: str,
    narrative_result: str,
    stats: Stats,
    inventory: list[InventoryItem]
) -> str:
    """构建Judge状态更新的用户提示词"""
    return f"""## 状态更新计算

### 事件描述
{event_context}

### 玩家选择的行动
{action_content}

### 判定叙事结果
{narrative_result}

### 玩家当前状态
{format_stats(stats)}

### 玩家背包
{format_inventory(inventory)}

---
请根据判定结果，计算状态和物品变化，并给出评分。
注意：消耗品使用后要在remove中扣除。"""


# ==================== Ending 提示词 (毒舌评论员/算命师) ====================

ENDING_SYSTEM_PROMPT = """你是一位末世生存游戏的结局评论员，扮演"毒舌算命师"角色。

## 你的人格特点
- 毒舌但有趣，评价一针见血
- 善于总结和提炼
- 喜欢用四字成语或网络热梗
- 对玩家的愚蠢行为会无情嘲讽

## 你的任务
玩家的游戏结束了（死亡或通关），你需要：
1. 分析死因（如果死亡）
2. 生成四字人设总结词（这是核心！）
3. 写一段毒舌评语
4. 生成五维雷达图数据

## 输出格式要求
必须返回以下JSON格式（不要有其他内容）：
```json
{
  "cause_of_death": "具体死因" 或 null（通关时为null）,
  "epithet": "四字总结",
  "comment": "毒舌评语...",
  "radar_chart": [战斗力, 生存力, 智慧, 运气, 心理素质]
}
```

## 人设词示例
- 囤货达人、末日战神、守财奴、倒霉蛋、铲屎官
- 老谋深算、心态爆炸、作死小能手、人间清醒

## 雷达图规则
- 每个维度0-10分
- 战斗力：根据战斗记录
- 生存力：根据存活天数和物资管理
- 智慧：根据决策质量
- 运气：根据随机事件结果
- 心理素质：根据SAN值变化"""


def build_ending_prompt(
    days_survived: int,
    high_light_moment: str,
    final_stats: Stats,
    final_inventory: list[InventoryItem],
    history: list[HistoryEntry]
) -> str:
    """
    构建Ending的用户提示词
    核心：提供完整的游戏回顾
    """
    # 结局时需要更多历史来做总结
    full_history = format_history(history, max_days=10)
    
    return f"""## 游戏结束

### 存活天数
{days_survived} 天

### 最终状态
{format_stats(final_stats)}

### 最终背包
{format_inventory(final_inventory)}

### 高光时刻
{high_light_moment if high_light_moment else "无特别高光时刻"}

### 完整经历回顾
{full_history}

---
请为这位玩家生成结局评价。
- 如果HP<=0，分析具体死因
- 如果存活超过30天，视为通关，cause_of_death为null
- 人设词要精准概括玩家的游戏风格
- 评语要毒舌但有趣"""
